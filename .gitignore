# ── OS / editor ──
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
CLAUDE.md

# ── Live data (never commit — contains real accounts/financial data) ──
data/
import/
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
uploads/*
preview/*
!uploads/.gitkeep

# ── Secrets / env ──
.env
.env.*
*.local

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ── Node / frontend tooling ──
node_modules/

# ── Runtime artifacts ──
*.pid
logs/

# ── Generated knowledge-graph output (graphify) ──
graphify-out/

# ── Local planning notes (not committed) ──
plans/

# ── Design system reference (local only) ──
DESIGN.md

# ── Playwright test artifacts ──
test-results/
# Local symlink → the claude-playwright container's test-results, so screenshots
# are browsable at nas:8088/shots/ via dev-server.py. Machine-specific, never commit.
frontend/shots
