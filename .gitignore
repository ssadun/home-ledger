# ── Live data (never commit — contains real accounts/financial data) ──
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
uploads/*
!uploads/.gitkeep

# ── Secrets / env ──
.env
.env.*
*.local

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ── Node / frontend tooling ──
node_modules/

# ── Runtime artifacts ──
*.pid
logs/

# ── OS / editor ──
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
OCR_TODO.md
PROGRESS.md
CLAUDE.md
